/** @type {import('tailwindcss').Config} */
export default {
  // No content array needed anymore — handled by @source in CSS
  theme: {
    extend: {
      // Your custom theme here if any
    },
  },
  plugins: [
    // Add any Tailwind plugins you use, e.g., require('@tailwindcss/forms')
  ],
}
